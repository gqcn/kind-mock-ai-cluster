This creates a mock AI cluster using Kind and installs Volcano for GPU scheduling.

You can manually change or add GPU nodes by modifying the `kind-1.27.yaml` and `mock-cluster-127.sh` files.

## Prerequisites

- Kind


## Usage

```sh
./mock-cluster-127.sh
```

